{
  "name": "ext-copyright",
  "displayName": "ext-copyright",
  "description": "description of extension copyright",
  "version": "0.0.2",
  "publisher": "MarcusS",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ext-copyright.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "ext-copyright.addCopyright",
        "title": "Update Copyright Header"
      }
    ],
    "configuration": {
      "title": "ext-copyright",
      "properties": {
        "ext-copyright.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the copyright header check on save."
        },
        "ext-copyright.headerText": {
          "type": [
            "string",
            "array"
          ],
          "items": {
            "type": "string"
          },
          "default": "// Copyright (c) 2025 My Company",
          "description": "The copyright header text. Can be a string (use \\n for newlines) or an array of strings."
        },
        "ext-copyright.confirmOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Enables a confirmation dialog before updating the copyright header on save."
        },
        "ext-copyright.fileExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "hpp",
            "cpp",
            "ts",
            "js",
            "py",
            "h"
          ],
          "description": "Only check files with these extensions (e.g. 'ts', 'js'). Leave empty to check all files."
        },
        "ext-copyright.extensionHeaders": {
          "type": "object",
          "default": {
            "hpp,cpp,h": [
              "/*!",
              "* Copyright (c) 2025 My Company ",
              "* more lines",
              "*/"
            ],
            "ts,js": "// Copyright (c) 2025 My Company",
            "py": "# Copyright (c) 2025 My Company"
          },
          "additionalProperties": {
            "type": [
              "string",
              "array"
            ],
            "items": {
              "type": "string"
            }
          },
          "description": "Map specific file extensions to copyright headers. Values can be strings or arrays of strings."
        }
      }
    }
  },
  
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^18.19.0",
    "typescript-eslint": "^8.48.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
